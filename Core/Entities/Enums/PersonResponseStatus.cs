using System;
using System.Collections.Generic;
using System.Text;

namespace Core.Entities.Enums
{
    //Follow convention on properties of entity to enum response status -- see name example
    //Change as needed -- this is just boilerplate
    public enum PersonResponseStatus
    {
        OK = 0,
        NameEmpty
    }
}
